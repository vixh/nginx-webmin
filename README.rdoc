Plugin for Virtualmin/Webmin.
Enable add/remove, disable sites to Nginx config

INSTALL
1. Copy to webmin directory(usual /usr/share/webmin)
2. Goto virtualmin -> Features and Plugins -> Nginx Configure